# Prototype reference

**Status:** The Mentor's Google AI Studio demo remains in AI Studio. No demo code, verified run instructions, or shared demo link has been added here yet. Continue onboarding from the [project brief](../README.md).

The demo will illustrate intended user journeys and interface behavior. Its code and apparent functionality require review before becoming an implementation baseline. A visible sign-in screen, answer, or citation is not evidence that authentication, retrieval, or permissions are implemented.

## Planned first import

The intended destination is `prototypes/google-ai-studio/`. When available, its README should record the export date/version, expected behavior, mocked features, limitations, dependencies, verified run instructions, and approved environment-variable names with no secret values.

Before sharing an export, inspect source/configuration/assets for credentials, private content, and generated dependencies. Import source and needed assets, not real environment files, installed dependencies, or build output. Keep demo dependencies separate from the future application.

## Continuing demo edits

Google's documentation currently describes both ZIP export and GitHub two-way sync. Availability and controls in this particular project still need inspection. See [Build apps in Google AI Studio](https://ai.google.dev/gemini-api/docs/aistudio-build-mode) (checked 14 September 2026).

The proposed first step is a reviewed snapshot import while the Mentor continues editing the original in AI Studio. Later snapshots should arrive as reviewable changes to the prototype folder, with a description of changed expectations.

Before enabling direct GitHub sync, verify its branch and folder behavior against this repository's review workflow. Do not assume it preserves the prototype subfolder or opens a PR automatically. Any integration must preserve shared documents and application work and avoid direct writes to protected `main`. No AI Studio connection or deployment is configured by these instructions.
